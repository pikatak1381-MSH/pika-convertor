/* import { calculatorRegistry } from "@/components/calculators/calculators.registry"

export const validateCalculators = () => {
    for (const category of categories) {
        for (const sub of category.subCategories) {
            if (!sub.id in calculatorRegistry) {
                throw new Error(
                    `Missing calculator implementation for: ${category.id}/${sub.id}`
                )
            }
        }
    }
} */
